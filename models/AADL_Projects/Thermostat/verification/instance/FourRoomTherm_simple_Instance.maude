mod FOURROOMTHERM_SIMPLE_INSTANCE-MODEL is
	including MODEL-TRANSITION-SYSTEM .
	
	--- AADL identifiers
	ops t a : -> VarId [ctor] .
	ops temp currtemp offcontrol tin oncontrol power tout tin1 tin2 : -> FeatureId [ctor] .
	ops FourRoomThermsimpleInstance ctrlThread ctrlProc env4 env3 env2 env1 p therm4 x therm3 therm2 therm1 : -> ComponentId [ctor] .
	ops init exec off on : -> Location [ctor] .
	ops loopBoundNum transBoundNum : -> Nat .
	--- the initial state
	op initial : -> Object .
	eq initial = < FourRoomThermsimpleInstance : System |
		features : (
			none),
		subcomponents : (
			< therm1 : System |
				features : (
					< power : DataOutPort | 
						content : null(Real) ,
						properties : (DataModel::InitialValue => {{0}}) >
					< tout : DataOutPort | 
						content : null(Real) ,
						properties : (DataModel::InitialValue => {{0}}) >
					< tin : DataInPort | 
						cache : null(Real),
						content : null(Real) ,
						properties : none >
					< currtemp : DataInPort | 
						cache : null(Real),
						content : null(Real) ,
						properties : none >
					< oncontrol : DataOutPort | 
						content : null(Unit) ,
						properties : none >
					< offcontrol : DataOutPort | 
						content : null(Unit) ,
						properties : none >),
				subcomponents : (
					< ctrlProc : Process |
						features : (
							< power : DataOutPort | 
								content : null(Real) ,
								properties : none >
							< tout : DataOutPort | 
								content : null(Real) ,
								properties : none >
							< tin : DataInPort | 
								cache : null(Real),
								content : null(Real) ,
								properties : none >
							< currtemp : DataInPort | 
								cache : null(Real),
								content : null(Real) ,
								properties : none >
							< oncontrol : DataOutPort | 
								content : null(Unit) ,
								properties : none >
							< offcontrol : DataOutPort | 
								content : null(Unit) ,
								properties : none >),
						subcomponents : (
							< ctrlThread : Thread |
								features : (
									< power : DataOutPort | 
										content : null(Real) ,
										properties : none >
									< tout : DataOutPort | 
										content : null(Real) ,
										properties : none >
									< tin : DataInPort | 
										cache : null(Real),
										content : null(Real) ,
										properties : none >
									< currtemp : DataInPort | 
										cache : null(Real),
										content : null(Real) ,
										properties : none >
									< oncontrol : DataOutPort | 
										content : null(Unit) ,
										properties : none >
									< offcontrol : DataOutPort | 
										content : null(Unit) ,
										properties : none >),
								subcomponents : (
									none),
								currState : (
									init),
								completeStates : (
									init),
								variables : (
									( a : Real )),
								transitions : (
									(init -[ on dispatch ]-> exec {
										(f{tout} := f[currtemp])
									}) ;
									(exec -[ [[true]] ]-> init {
										(v{a} := minus((f[currtemp] - f[tin]))) ;
										if ((v[a] > [[10]])){
											{
												(f{power} := [[5]]) ;
												(oncontrol !)
											}
										}
										(
										(elsif ((v[a] > [[3]])){
											{
												(f{power} := [[3]]) ;
												(oncontrol !)
											}
										})
										)
										else {
											{
												(f{power} := [[0]]) ;
												(offcontrol !)
											}
										}
										end if
									})),
								loopBound : (
									loopBoundNum),
								transBound : (
									transBoundNum),
								varGen : (
									< "FourRoomThermsimpleInstance.therm1.ctrlProc.ctrlThread" >
									),
								properties : (
									(TimingProperties::Period => {{100.0}}) ;
									(HybridSynchAADL::MaxClockDeviation => {{5.0}}) ;
									(HybridSynchAADL::SamplingTime => {{20.0 .. 30.0}}) ;
									(HybridSynchAADL::ResponseTime => {{60.0 .. 80.0}}) ;
									(HybridSynchAADL::Synchronous => {{true}})),
								connections : (
									empty) 
								> ),
						properties : (
							(TimingProperties::Period => {{100.0}}) ;
							(HybridSynchAADL::MaxClockDeviation => {{5.0}}) ;
							(HybridSynchAADL::SamplingTime => {{20.0 .. 30.0}}) ;
							(HybridSynchAADL::ResponseTime => {{60.0 .. 80.0}}) ;
							(HybridSynchAADL::Synchronous => {{true}})),
						connections : (
							(ctrlThread .. oncontrol --> oncontrol) ;
							(ctrlThread .. power --> power) ;
							(ctrlThread .. tout --> tout) ;
							(tin --> ctrlThread .. tin) ;
							(ctrlThread .. offcontrol --> offcontrol) ;
							(currtemp --> ctrlThread .. currtemp)) 
						> ),
				properties : (
					(TimingProperties::Period => {{100.0}}) ;
					(HybridSynchAADL::MaxClockDeviation => {{5.0}}) ;
					(HybridSynchAADL::SamplingTime => {{20.0 .. 30.0}}) ;
					(HybridSynchAADL::ResponseTime => {{60.0 .. 80.0}}) ;
					(HybridSynchAADL::Synchronous => {{true}})),
				connections : (
					(ctrlProc .. tout --> tout) ;
					(ctrlProc .. oncontrol --> oncontrol) ;
					(currtemp --> ctrlProc .. currtemp) ;
					(ctrlProc .. power --> power) ;
					(tin --> ctrlProc .. tin) ;
					(ctrlProc .. offcontrol --> offcontrol)) 
				> 
			< therm2 : System |
				features : (
					< power : DataOutPort | 
						content : null(Real) ,
						properties : (DataModel::InitialValue => {{0}}) >
					< tout : DataOutPort | 
						content : null(Real) ,
						properties : (DataModel::InitialValue => {{0}}) >
					< tin1 : DataInPort | 
						cache : null(Real),
						content : null(Real) ,
						properties : none >
					< tin2 : DataInPort | 
						cache : null(Real),
						content : null(Real) ,
						properties : none >
					< currtemp : DataInPort | 
						cache : null(Real),
						content : null(Real) ,
						properties : none >
					< oncontrol : DataOutPort | 
						content : null(Unit) ,
						properties : none >
					< offcontrol : DataOutPort | 
						content : null(Unit) ,
						properties : none >),
				subcomponents : (
					< ctrlProc : Process |
						features : (
							< power : DataOutPort | 
								content : null(Real) ,
								properties : none >
							< tout : DataOutPort | 
								content : null(Real) ,
								properties : none >
							< tin1 : DataInPort | 
								cache : null(Real),
								content : null(Real) ,
								properties : none >
							< tin2 : DataInPort | 
								cache : null(Real),
								content : null(Real) ,
								properties : none >
							< currtemp : DataInPort | 
								cache : null(Real),
								content : null(Real) ,
								properties : none >
							< oncontrol : DataOutPort | 
								content : null(Unit) ,
								properties : none >
							< offcontrol : DataOutPort | 
								content : null(Unit) ,
								properties : none >),
						subcomponents : (
							< ctrlThread : Thread |
								features : (
									< power : DataOutPort | 
										content : null(Real) ,
										properties : none >
									< tout : DataOutPort | 
										content : null(Real) ,
										properties : none >
									< tin1 : DataInPort | 
										cache : null(Real),
										content : null(Real) ,
										properties : none >
									< tin2 : DataInPort | 
										cache : null(Real),
										content : null(Real) ,
										properties : none >
									< currtemp : DataInPort | 
										cache : null(Real),
										content : null(Real) ,
										properties : none >
									< oncontrol : DataOutPort | 
										content : null(Unit) ,
										properties : none >
									< offcontrol : DataOutPort | 
										content : null(Unit) ,
										properties : none >),
								subcomponents : (
									none),
								currState : (
									init),
								completeStates : (
									init),
								variables : (
									( a : Real )),
								transitions : (
									(init -[ on dispatch ]-> exec {
										(f{tout} := f[currtemp])
									}) ;
									(exec -[ [[true]] ]-> init {
										(v{a} := minus((f[currtemp] - f[tin1]))) ;
										(v{a} := (v[a] - (f[currtemp] - f[tin2]))) ;
										if ((v[a] > [[10]])){
											{
												(f{power} := [[5]]) ;
												(oncontrol !)
											}
										}
										(
										(elsif ((v[a] > [[3]])){
											{
												(f{power} := [[3]]) ;
												(oncontrol !)
											}
										})
										)
										else {
											{
												(f{power} := [[0]]) ;
												(offcontrol !)
											}
										}
										end if
									})),
								loopBound : (
									loopBoundNum),
								transBound : (
									transBoundNum),
								varGen : (
									< "FourRoomThermsimpleInstance.therm2.ctrlProc.ctrlThread" >
									),
								properties : (
									(TimingProperties::Period => {{100.0}}) ;
									(HybridSynchAADL::MaxClockDeviation => {{5.0}}) ;
									(HybridSynchAADL::SamplingTime => {{20.0 .. 30.0}}) ;
									(HybridSynchAADL::ResponseTime => {{60.0 .. 80.0}}) ;
									(HybridSynchAADL::Synchronous => {{true}})),
								connections : (
									empty) 
								> ),
						properties : (
							(TimingProperties::Period => {{100.0}}) ;
							(HybridSynchAADL::MaxClockDeviation => {{5.0}}) ;
							(HybridSynchAADL::SamplingTime => {{20.0 .. 30.0}}) ;
							(HybridSynchAADL::ResponseTime => {{60.0 .. 80.0}}) ;
							(HybridSynchAADL::Synchronous => {{true}})),
						connections : (
							(ctrlThread .. power --> power) ;
							(ctrlThread .. offcontrol --> offcontrol) ;
							(tin2 --> ctrlThread .. tin2) ;
							(ctrlThread .. tout --> tout) ;
							(tin1 --> ctrlThread .. tin1) ;
							(ctrlThread .. tout --> tout) ;
							(ctrlThread .. oncontrol --> oncontrol) ;
							(currtemp --> ctrlThread .. currtemp)) 
						> ),
				properties : (
					(TimingProperties::Period => {{100.0}}) ;
					(HybridSynchAADL::MaxClockDeviation => {{5.0}}) ;
					(HybridSynchAADL::SamplingTime => {{20.0 .. 30.0}}) ;
					(HybridSynchAADL::ResponseTime => {{60.0 .. 80.0}}) ;
					(HybridSynchAADL::Synchronous => {{true}})),
				connections : (
					(tin1 --> ctrlProc .. tin1) ;
					(tin2 --> ctrlProc .. tin2) ;
					(ctrlProc .. power --> power) ;
					(ctrlProc .. offcontrol --> offcontrol) ;
					(currtemp --> ctrlProc .. currtemp) ;
					(ctrlProc .. tout --> tout) ;
					(ctrlProc .. tout --> tout) ;
					(ctrlProc .. oncontrol --> oncontrol)) 
				> 
			< therm3 : System |
				features : (
					< power : DataOutPort | 
						content : null(Real) ,
						properties : (DataModel::InitialValue => {{0}}) >
					< tout : DataOutPort | 
						content : null(Real) ,
						properties : (DataModel::InitialValue => {{0}}) >
					< tin1 : DataInPort | 
						cache : null(Real),
						content : null(Real) ,
						properties : none >
					< tin2 : DataInPort | 
						cache : null(Real),
						content : null(Real) ,
						properties : none >
					< currtemp : DataInPort | 
						cache : null(Real),
						content : null(Real) ,
						properties : none >
					< oncontrol : DataOutPort | 
						content : null(Unit) ,
						properties : none >
					< offcontrol : DataOutPort | 
						content : null(Unit) ,
						properties : none >),
				subcomponents : (
					< ctrlProc : Process |
						features : (
							< power : DataOutPort | 
								content : null(Real) ,
								properties : none >
							< tout : DataOutPort | 
								content : null(Real) ,
								properties : none >
							< tin1 : DataInPort | 
								cache : null(Real),
								content : null(Real) ,
								properties : none >
							< tin2 : DataInPort | 
								cache : null(Real),
								content : null(Real) ,
								properties : none >
							< currtemp : DataInPort | 
								cache : null(Real),
								content : null(Real) ,
								properties : none >
							< oncontrol : DataOutPort | 
								content : null(Unit) ,
								properties : none >
							< offcontrol : DataOutPort | 
								content : null(Unit) ,
								properties : none >),
						subcomponents : (
							< ctrlThread : Thread |
								features : (
									< power : DataOutPort | 
										content : null(Real) ,
										properties : none >
									< tout : DataOutPort | 
										content : null(Real) ,
										properties : none >
									< tin1 : DataInPort | 
										cache : null(Real),
										content : null(Real) ,
										properties : none >
									< tin2 : DataInPort | 
										cache : null(Real),
										content : null(Real) ,
										properties : none >
									< currtemp : DataInPort | 
										cache : null(Real),
										content : null(Real) ,
										properties : none >
									< oncontrol : DataOutPort | 
										content : null(Unit) ,
										properties : none >
									< offcontrol : DataOutPort | 
										content : null(Unit) ,
										properties : none >),
								subcomponents : (
									none),
								currState : (
									init),
								completeStates : (
									init),
								variables : (
									( a : Real )),
								transitions : (
									(init -[ on dispatch ]-> exec {
										(f{tout} := f[currtemp])
									}) ;
									(exec -[ [[true]] ]-> init {
										(v{a} := minus((f[currtemp] - f[tin1]))) ;
										(v{a} := (v[a] - (f[currtemp] - f[tin2]))) ;
										if ((v[a] > [[10]])){
											{
												(f{power} := [[5]]) ;
												(oncontrol !)
											}
										}
										(
										(elsif ((v[a] > [[3]])){
											{
												(f{power} := [[3]]) ;
												(oncontrol !)
											}
										})
										)
										else {
											{
												(f{power} := [[0]]) ;
												(offcontrol !)
											}
										}
										end if
									})),
								loopBound : (
									loopBoundNum),
								transBound : (
									transBoundNum),
								varGen : (
									< "FourRoomThermsimpleInstance.therm3.ctrlProc.ctrlThread" >
									),
								properties : (
									(TimingProperties::Period => {{100.0}}) ;
									(HybridSynchAADL::MaxClockDeviation => {{5.0}}) ;
									(HybridSynchAADL::SamplingTime => {{20.0 .. 30.0}}) ;
									(HybridSynchAADL::ResponseTime => {{60.0 .. 80.0}}) ;
									(HybridSynchAADL::Synchronous => {{true}})),
								connections : (
									empty) 
								> ),
						properties : (
							(TimingProperties::Period => {{100.0}}) ;
							(HybridSynchAADL::MaxClockDeviation => {{5.0}}) ;
							(HybridSynchAADL::SamplingTime => {{20.0 .. 30.0}}) ;
							(HybridSynchAADL::ResponseTime => {{60.0 .. 80.0}}) ;
							(HybridSynchAADL::Synchronous => {{true}})),
						connections : (
							(tin1 --> ctrlThread .. tin1) ;
							(ctrlThread .. offcontrol --> offcontrol) ;
							(currtemp --> ctrlThread .. currtemp) ;
							(ctrlThread .. oncontrol --> oncontrol) ;
							(ctrlThread .. tout --> tout) ;
							(ctrlThread .. tout --> tout) ;
							(ctrlThread .. power --> power) ;
							(tin2 --> ctrlThread .. tin2)) 
						> ),
				properties : (
					(TimingProperties::Period => {{100.0}}) ;
					(HybridSynchAADL::MaxClockDeviation => {{5.0}}) ;
					(HybridSynchAADL::SamplingTime => {{20.0 .. 30.0}}) ;
					(HybridSynchAADL::ResponseTime => {{60.0 .. 80.0}}) ;
					(HybridSynchAADL::Synchronous => {{true}})),
				connections : (
					(ctrlProc .. power --> power) ;
					(currtemp --> ctrlProc .. currtemp) ;
					(ctrlProc .. offcontrol --> offcontrol) ;
					(ctrlProc .. oncontrol --> oncontrol) ;
					(tin1 --> ctrlProc .. tin1) ;
					(tin2 --> ctrlProc .. tin2) ;
					(ctrlProc .. tout --> tout) ;
					(ctrlProc .. tout --> tout)) 
				> 
			< therm4 : System |
				features : (
					< power : DataOutPort | 
						content : null(Real) ,
						properties : (DataModel::InitialValue => {{0}}) >
					< tout : DataOutPort | 
						content : null(Real) ,
						properties : (DataModel::InitialValue => {{0}}) >
					< tin : DataInPort | 
						cache : null(Real),
						content : null(Real) ,
						properties : none >
					< currtemp : DataInPort | 
						cache : null(Real),
						content : null(Real) ,
						properties : none >
					< oncontrol : DataOutPort | 
						content : null(Unit) ,
						properties : none >
					< offcontrol : DataOutPort | 
						content : null(Unit) ,
						properties : none >),
				subcomponents : (
					< ctrlProc : Process |
						features : (
							< power : DataOutPort | 
								content : null(Real) ,
								properties : none >
							< tout : DataOutPort | 
								content : null(Real) ,
								properties : none >
							< tin : DataInPort | 
								cache : null(Real),
								content : null(Real) ,
								properties : none >
							< currtemp : DataInPort | 
								cache : null(Real),
								content : null(Real) ,
								properties : none >
							< oncontrol : DataOutPort | 
								content : null(Unit) ,
								properties : none >
							< offcontrol : DataOutPort | 
								content : null(Unit) ,
								properties : none >),
						subcomponents : (
							< ctrlThread : Thread |
								features : (
									< power : DataOutPort | 
										content : null(Real) ,
										properties : none >
									< tout : DataOutPort | 
										content : null(Real) ,
										properties : none >
									< tin : DataInPort | 
										cache : null(Real),
										content : null(Real) ,
										properties : none >
									< currtemp : DataInPort | 
										cache : null(Real),
										content : null(Real) ,
										properties : none >
									< oncontrol : DataOutPort | 
										content : null(Unit) ,
										properties : none >
									< offcontrol : DataOutPort | 
										content : null(Unit) ,
										properties : none >),
								subcomponents : (
									none),
								currState : (
									init),
								completeStates : (
									init),
								variables : (
									( a : Real )),
								transitions : (
									(init -[ on dispatch ]-> exec {
										(f{tout} := f[currtemp])
									}) ;
									(exec -[ [[true]] ]-> init {
										(v{a} := minus((f[currtemp] - f[tin]))) ;
										if ((v[a] > [[10]])){
											{
												(f{power} := [[5]]) ;
												(oncontrol !)
											}
										}
										(
										(elsif ((v[a] > [[3]])){
											{
												(f{power} := [[3]]) ;
												(oncontrol !)
											}
										})
										)
										else {
											{
												(f{power} := [[0]]) ;
												(offcontrol !)
											}
										}
										end if
									})),
								loopBound : (
									loopBoundNum),
								transBound : (
									transBoundNum),
								varGen : (
									< "FourRoomThermsimpleInstance.therm4.ctrlProc.ctrlThread" >
									),
								properties : (
									(TimingProperties::Period => {{100.0}}) ;
									(HybridSynchAADL::MaxClockDeviation => {{5.0}}) ;
									(HybridSynchAADL::SamplingTime => {{20.0 .. 30.0}}) ;
									(HybridSynchAADL::ResponseTime => {{60.0 .. 80.0}}) ;
									(HybridSynchAADL::Synchronous => {{true}})),
								connections : (
									empty) 
								> ),
						properties : (
							(TimingProperties::Period => {{100.0}}) ;
							(HybridSynchAADL::MaxClockDeviation => {{5.0}}) ;
							(HybridSynchAADL::SamplingTime => {{20.0 .. 30.0}}) ;
							(HybridSynchAADL::ResponseTime => {{60.0 .. 80.0}}) ;
							(HybridSynchAADL::Synchronous => {{true}})),
						connections : (
							(currtemp --> ctrlThread .. currtemp) ;
							(tin --> ctrlThread .. tin) ;
							(ctrlThread .. power --> power) ;
							(ctrlThread .. tout --> tout) ;
							(ctrlThread .. offcontrol --> offcontrol) ;
							(ctrlThread .. oncontrol --> oncontrol)) 
						> ),
				properties : (
					(TimingProperties::Period => {{100.0}}) ;
					(HybridSynchAADL::MaxClockDeviation => {{5.0}}) ;
					(HybridSynchAADL::SamplingTime => {{20.0 .. 30.0}}) ;
					(HybridSynchAADL::ResponseTime => {{60.0 .. 80.0}}) ;
					(HybridSynchAADL::Synchronous => {{true}})),
				connections : (
					(ctrlProc .. offcontrol --> offcontrol) ;
					(currtemp --> ctrlProc .. currtemp) ;
					(tin --> ctrlProc .. tin) ;
					(ctrlProc .. tout --> tout) ;
					(ctrlProc .. oncontrol --> oncontrol) ;
					(ctrlProc .. power --> power)) 
				> 
			< env1 : Env |
				features : (
					< temp : EnvOutPort | 
						target : ctrlThread,
						envCache : null(Real),
						content : null(Real) ,
						properties : (DataModel::InitialValue => {{0}}) >
					< power : EnvInPort | 
						target : ctrlThread,
						envCache : null(Real),
						content : null(Real) ,
						properties : none >
					< oncontrol : EnvInPort | 
						target : ctrlThread,
						envCache : null(Unit),
						content : null(Unit) ,
						properties : none >
					< offcontrol : EnvInPort | 
						target : ctrlThread,
						envCache : null(Unit),
						content : null(Unit) ,
						properties : none >),
				subcomponents : (
					< x : Data |
						features : (
							none),
						subcomponents : (
							none),
						value : param(Real),
						properties : (
							none),
						connections : (
							empty) 
						> 
					< p : Data |
						features : (
							none),
						subcomponents : (
							none),
						value : null(Real),
						properties : (
							(DataModel::InitialValue => {{0}})),
						connections : (
							empty) 
						> ),
				currMode : (
					off
					),
				jumps : (
					(off -[ (oncontrol) ]-> on) ;
					(off -[ (offcontrol) ]-> off) ;
					(on -[ (oncontrol) ]-> on) ;
					(on -[ (offcontrol) ]-> off)
					),
				flows : (
					((on)[(x(t) = (c[x] + (([[0.01]] * c[p]) * v[t]))) ] ) ;
					((off)[(x(t) = (c[x] - ([[0.001]] * v[t]))) ] )
					),
				sampling : (
					(ctrlThread : (rat(20.0),rat(30.0)))
					),
				response : (
					(ctrlThread : (rat(60.0),rat(80.0)))
					),
				varGen : (
					< "FourRoomThermsimpleInstance.env1" >
					),
				properties : (
					(TimingProperties::Period => {{100.0}}) ;
					(HybridSynchAADL::MaxClockDeviation => {{5.0}}) ;
					(HybridSynchAADL::Synchronous => {{true}}) ;
					(HybridSynchAADL::ContinuousDynamics => {{"x(t) = x(0) - (0.001 * t);"}}) ;
					(HybridSynchAADL::isEnvironment => {{true}})),
				connections : (
					(power =>> p) ;
					(x ==> temp)) 
				> 
			< env2 : Env |
				features : (
					< temp : EnvOutPort | 
						target : ctrlThread,
						envCache : null(Real),
						content : null(Real) ,
						properties : (DataModel::InitialValue => {{0}}) >
					< power : EnvInPort | 
						target : ctrlThread,
						envCache : null(Real),
						content : null(Real) ,
						properties : none >
					< oncontrol : EnvInPort | 
						target : ctrlThread,
						envCache : null(Unit),
						content : null(Unit) ,
						properties : none >
					< offcontrol : EnvInPort | 
						target : ctrlThread,
						envCache : null(Unit),
						content : null(Unit) ,
						properties : none >),
				subcomponents : (
					< x : Data |
						features : (
							none),
						subcomponents : (
							none),
						value : param(Real),
						properties : (
							none),
						connections : (
							empty) 
						> 
					< p : Data |
						features : (
							none),
						subcomponents : (
							none),
						value : null(Real),
						properties : (
							(DataModel::InitialValue => {{0}})),
						connections : (
							empty) 
						> ),
				currMode : (
					off
					),
				jumps : (
					(off -[ (oncontrol) ]-> on) ;
					(off -[ (offcontrol) ]-> off) ;
					(on -[ (oncontrol) ]-> on) ;
					(on -[ (offcontrol) ]-> off)
					),
				flows : (
					((on)[(x(t) = (c[x] + (([[0.01]] * c[p]) * v[t]))) ] ) ;
					((off)[(x(t) = (c[x] - ([[0.001]] * v[t]))) ] )
					),
				sampling : (
					(ctrlThread : (rat(20.0),rat(30.0)))
					),
				response : (
					(ctrlThread : (rat(60.0),rat(80.0)))
					),
				varGen : (
					< "FourRoomThermsimpleInstance.env2" >
					),
				properties : (
					(TimingProperties::Period => {{100.0}}) ;
					(HybridSynchAADL::MaxClockDeviation => {{5.0}}) ;
					(HybridSynchAADL::Synchronous => {{true}}) ;
					(HybridSynchAADL::ContinuousDynamics => {{"x(t) = x(0) - (0.001 * t);"}}) ;
					(HybridSynchAADL::isEnvironment => {{true}})),
				connections : (
					(power =>> p) ;
					(x ==> temp)) 
				> 
			< env3 : Env |
				features : (
					< temp : EnvOutPort | 
						target : ctrlThread,
						envCache : null(Real),
						content : null(Real) ,
						properties : (DataModel::InitialValue => {{0}}) >
					< power : EnvInPort | 
						target : ctrlThread,
						envCache : null(Real),
						content : null(Real) ,
						properties : none >
					< oncontrol : EnvInPort | 
						target : ctrlThread,
						envCache : null(Unit),
						content : null(Unit) ,
						properties : none >
					< offcontrol : EnvInPort | 
						target : ctrlThread,
						envCache : null(Unit),
						content : null(Unit) ,
						properties : none >),
				subcomponents : (
					< x : Data |
						features : (
							none),
						subcomponents : (
							none),
						value : param(Real),
						properties : (
							none),
						connections : (
							empty) 
						> 
					< p : Data |
						features : (
							none),
						subcomponents : (
							none),
						value : null(Real),
						properties : (
							(DataModel::InitialValue => {{0}})),
						connections : (
							empty) 
						> ),
				currMode : (
					off
					),
				jumps : (
					(off -[ (oncontrol) ]-> on) ;
					(off -[ (offcontrol) ]-> off) ;
					(on -[ (oncontrol) ]-> on) ;
					(on -[ (offcontrol) ]-> off)
					),
				flows : (
					((on)[(x(t) = (c[x] + (([[0.01]] * c[p]) * v[t]))) ] ) ;
					((off)[(x(t) = (c[x] - ([[0.001]] * v[t]))) ] )
					),
				sampling : (
					(ctrlThread : (rat(20.0),rat(30.0)))
					),
				response : (
					(ctrlThread : (rat(60.0),rat(80.0)))
					),
				varGen : (
					< "FourRoomThermsimpleInstance.env3" >
					),
				properties : (
					(TimingProperties::Period => {{100.0}}) ;
					(HybridSynchAADL::MaxClockDeviation => {{5.0}}) ;
					(HybridSynchAADL::Synchronous => {{true}}) ;
					(HybridSynchAADL::ContinuousDynamics => {{"x(t) = x(0) - (0.001 * t);"}}) ;
					(HybridSynchAADL::isEnvironment => {{true}})),
				connections : (
					(x ==> temp) ;
					(power =>> p)) 
				> 
			< env4 : Env |
				features : (
					< temp : EnvOutPort | 
						target : ctrlThread,
						envCache : null(Real),
						content : null(Real) ,
						properties : (DataModel::InitialValue => {{0}}) >
					< power : EnvInPort | 
						target : ctrlThread,
						envCache : null(Real),
						content : null(Real) ,
						properties : none >
					< oncontrol : EnvInPort | 
						target : ctrlThread,
						envCache : null(Unit),
						content : null(Unit) ,
						properties : none >
					< offcontrol : EnvInPort | 
						target : ctrlThread,
						envCache : null(Unit),
						content : null(Unit) ,
						properties : none >),
				subcomponents : (
					< x : Data |
						features : (
							none),
						subcomponents : (
							none),
						value : param(Real),
						properties : (
							none),
						connections : (
							empty) 
						> 
					< p : Data |
						features : (
							none),
						subcomponents : (
							none),
						value : null(Real),
						properties : (
							(DataModel::InitialValue => {{0}})),
						connections : (
							empty) 
						> ),
				currMode : (
					off
					),
				jumps : (
					(off -[ (oncontrol) ]-> on) ;
					(off -[ (offcontrol) ]-> off) ;
					(on -[ (oncontrol) ]-> on) ;
					(on -[ (offcontrol) ]-> off)
					),
				flows : (
					((on)[(x(t) = (c[x] + (([[0.01]] * c[p]) * v[t]))) ] ) ;
					((off)[(x(t) = (c[x] - ([[0.001]] * v[t]))) ] )
					),
				sampling : (
					(ctrlThread : (rat(20.0),rat(30.0)))
					),
				response : (
					(ctrlThread : (rat(60.0),rat(80.0)))
					),
				varGen : (
					< "FourRoomThermsimpleInstance.env4" >
					),
				properties : (
					(TimingProperties::Period => {{100.0}}) ;
					(HybridSynchAADL::MaxClockDeviation => {{5.0}}) ;
					(HybridSynchAADL::Synchronous => {{true}}) ;
					(HybridSynchAADL::ContinuousDynamics => {{"x(t) = x(0) - (0.001 * t);"}}) ;
					(HybridSynchAADL::isEnvironment => {{true}})),
				connections : (
					(power =>> p) ;
					(x ==> temp)) 
				> ),
		properties : (
			(TimingProperties::Period => {{100.0}}) ;
			(HybridSynchAADL::MaxClockDeviation => {{5.0}}) ;
			(HybridSynchAADL::Synchronous => {{true}})),
		connections : (
			(env2 .. temp --> therm2 .. currtemp) ;
			(env3 .. temp --> therm3 .. currtemp) ;
			(therm4 .. offcontrol --> env4 .. offcontrol) ;
			(therm2 .. power --> env2 .. power) ;
			(therm4 .. tout --> therm3 .. tin2) ;
			(therm4 .. oncontrol --> env4 .. oncontrol) ;
			(therm2 .. offcontrol --> env2 .. offcontrol) ;
			(therm1 .. offcontrol --> env1 .. offcontrol) ;
			(env4 .. temp --> therm4 .. currtemp) ;
			(therm1 .. power --> env1 .. power) ;
			(therm3 .. tout --> therm2 .. tin2) ;
			(therm3 .. tout --> therm4 .. tin) ;
			(therm2 .. oncontrol --> env2 .. oncontrol) ;
			(therm3 .. oncontrol --> env3 .. oncontrol) ;
			(env1 .. temp --> therm1 .. currtemp) ;
			(therm3 .. power --> env3 .. power) ;
			(therm2 .. tout --> therm3 .. tin1) ;
			(therm2 .. tout --> therm1 .. tin) ;
			(therm3 .. offcontrol --> env3 .. offcontrol) ;
			(therm1 .. tout --> therm2 .. tin1) ;
			(therm1 .. oncontrol --> env1 .. oncontrol) ;
			(therm4 .. power --> env4 .. power)) 
		>  .
endm
