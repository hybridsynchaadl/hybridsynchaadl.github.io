load /Users/jaehun/Documents/HybridSynchAADL/maude/prelude.maude
load /Users/jaehun/Documents/HybridSynchAADL/maude/smt.maude
load ../semantics/interpreter-symbolic2-merge.maude
load ../instance/TwoDronesSystem_form1D_Instance-symbolic.maude
mod TEST-TWODRONESSYSTEM_FORM1D_INSTANCE is
  including TWODRONESSYSTEM_FORM1D_INSTANCE-MODEL-SYMBOLIC .
  including REACHABILITY-ANALYSIS .
  including SPECIFICATION-LANGUAGE-SEMANTICS .

  op initState : -> Object .
  eq initState = initialize(collapse(initial)) .
  
  eq loopBoundNum = 10 .
  eq transBoundNum = 10 .
  
  
  eq @m@ = ['TEST-TWODRONESSYSTEM_FORM1D_INSTANCE] .
  
  op initial : -> PropSpec .
  eq initial = (((abs(((drone1 . environment | c[x] ) - [[0.0]])) < [[0.01]]) and (abs(((drone2 . environment | c[x] ) - [[0.5]])) < [[0.01]])) and (abs(((refDrone . env | c[x] ) - [[0.3]])) < [[0.01]])) .
  op twoClose : -> PropSpec .
  eq twoClose = (abs(((drone1 . environment | c[x] ) - (drone2 . environment | c[x] ))) < [[0.3]]) .
  
endm
	  
red reachAnalysis(
    initState,
    (TwoDronesSystemform1DInstance | initial),
    TwoDronesSystemform1DInstance | not(not(twoClose)),
    500
    ) .

quit
